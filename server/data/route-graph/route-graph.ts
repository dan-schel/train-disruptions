/**
 * Used by the route finding algorithm to determine whether a particular commute
 * is achievable by train, requires buses, etc. and the route the passenger
 * needs to take. (See docs/line-routes.md for more info.)
 */
export class RouteGraph {
  // prettier-ignore
  constructor(
    // TODO: Something like this
    // readonly edges: readonly RouteGraphEdge[],
    // readonly nodes: readonly RouteGraphNode[],
  ) {}
}
